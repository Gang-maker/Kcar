{{! filepath: c:\Users\DELL\Desktop\Kcar - Copy\Kcarexport\views\admin\electric_list.hbs }}
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Electric Vehicles | Kcarexport</title>
    
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css">
    
    <style>
        body {
            font-family: 'Open Sans', sans-serif;
            background-color: #f8f9fa;
        }
        
        .table-title {
            margin-bottom: 1.5rem;
            color: #2c3e50;
        }
        
        .btn-view {
            margin-right: 0.5rem;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1 class="table-title">Electric Vehicles</h1>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Manufacturer</th>
                    <th>Model</th>
                    <th>Year</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {{#each electricVehicles}}
                <tr>
                    <td>{{this.id}}</td>
                    <td>{{this.manufacturer}}</td>
                    <td>{{this.model}}</td>
                    <td>{{this.year}}</td>
                    <td>
                        {{#if this.external}}
                        <a class="btn btn-view action-btn" href="/electric/booknow/external/{{this.id}}">
                            <i class="fas fa-eye"></i> View
                        </a>
                        {{else}}
                        <a class="btn btn-view action-btn" href="/electric/booknow/{{this._id}}">
                            <i class="fas fa-eye"></i> View
                        </a>
                        {{/if}}
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>

    <!-- Bootstrap & jQuery JS -->
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>