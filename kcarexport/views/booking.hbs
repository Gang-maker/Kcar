<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{model.manufacturer}} {{model.model}} | Kcarexport</title>
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css">
    
    <style>
        :root {
            --primary: #2c3e50;
            --secondary: #3498db;
            --accent: #e74c3c;
            --light: #f8f9fa;
            --dark: #343a40;
            --gray: #6c757d;
            --light-gray: #e9ecef;
            --border-radius: 8px;
            --box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            --transition: all 0.3s ease;
        }
        
        body {
            font-family: 'Open Sans', sans-serif;
            color: #333;
            line-height: 1.6;
            background-color: #f8f9fa;
        }
        
        h1, h2, h3, h4, h5, h6 {
            font-family: 'Montserrat', sans-serif;
            font-weight: 600;
        }
        
        .car-title {
            font-size: 2.2rem;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: 0.5rem;
        }
        
        .car-subtitle {
            font-size: 1.3rem;
            color: var(--gray);
            margin-bottom: 1.5rem;
        }
        
        .carousel-container {
            border-radius: var(--border-radius);
            overflow: hidden;
            box-shadow: var(--box-shadow);
            margin-bottom: 2.5rem;
            background: white;
            padding: 10px;
        }
        
        .carousel-item img {
            height: 400px;
            object-fit: cover;
            border-radius: var(--border-radius);
        }
        
        .carousel-control-prev, 
        .carousel-control-next {
            width: 50px;
            height: 50px;
            background: rgba(0,0,0,0.3);
            border-radius: 50%;
            top: 50%;
            transform: translateY(-50%);
            opacity: 0.8;
            transition: var(--transition);
        }
        
        .carousel-control-prev:hover, 
        .carousel-control-next:hover {
            opacity: 1;
            background: var(--primary);
        }
        
        .details-card {
            background: white;
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow);
            padding: 2rem;
            margin-bottom: 2rem;
        }
        
        .price-tag {
            font-size: 2rem;
            font-weight: 700;
            color: var(--accent);
            margin-bottom: 1.5rem;
        }
        
        .car-details-list {
            list-style: none;
            padding: 0;
            margin: 0 0 1.5rem 0;
        }
        
        .car-details-list li {
            display: flex;
            align-items: center;
            margin-bottom: 1rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid var(--light-gray);
        }
        
        .car-details-list li:last-child {
            border-bottom: none;
            margin-bottom: 0;
            padding-bottom: 0;
        }
        
        .car-details-list svg {
            margin-right: 1rem;
            color: var(--secondary);
            min-width: 24px;
            font-size: 1.2rem;
        }
        
        .car-details-list b {
            margin-right: 0.5rem;
            color: var(--primary);
            min-width: 100px;
            display: inline-block;
        }
        
        .options-section {
            background: white;
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow);
            padding: 2rem;
            margin-bottom: 2rem;
        }
        
        .options-title {
            font-size: 1.5rem;
            margin-bottom: 1.5rem;
            color: var(--primary);
            position: relative;
            padding-bottom: 0.5rem;
        }
        
        .options-title::after {
            content: '';
            position: absolute;
            left: 0;
            bottom: 0;
            width: 50px;
            height: 3px;
            background: var(--secondary);
        }
        
        .badge-option {
            background: var(--secondary);
            color: white;
            margin: 0 0.5rem 0.5rem 0;
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-size: 0.9rem;
            display: inline-block;
            transition: var(--transition);
        }
        
        .badge-option:hover {
            transform: translateY(-2px);
            box-shadow: 0 3px 10px rgba(0,0,0,0.1);
        }
        
        .btn-order {
            background: var(--accent);
            color: white;
            padding: 0.8rem 2.5rem;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1.1rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: var(--transition);
            border: none;
            display: inline-block;
            margin-top: 1rem;
            box-shadow: 0 4px 15px rgba(231, 76, 60, 0.3);
        }
        
        .btn-order:hover {
            transform: translateY(-3px);
            box-shadow: 0 7px 20px rgba(231, 76, 60, 0.4);
            color: white;
            background: #c0392b;
        }
        
        .modal-content {
            border-radius: var(--border-radius);
            border: none;
        }
        
        .modal-header {
            border-bottom: none;
            padding-bottom: 0;
        }
        
        .modal-title {
            font-weight: 700;
            color: var(--primary);
        }
        
        .modal-body {
            padding-top: 0;
        }
        
        .form-control {
            border-radius: var(--border-radius);
            padding: 0.8rem 1rem;
            border: 1px solid var(--light-gray);
        }
        
        .form-control:focus {
            border-color: var(--secondary);
            box-shadow: 0 0 0 0.2rem rgba(52, 152, 219, 0.25);
        }
        
        .btn-secondary {
            background: var(--secondary);
            border: none;
            border-radius: var(--border-radius);
            padding: 0.6rem 1.5rem;
            transition: var(--transition);
        }
        
        .btn-secondary:hover {
            background: #2980b9;
        }
        
        @media (max-width: 768px) {
            .car-title {
                font-size: 1.8rem;
            }
            
            .car-subtitle {
                font-size: 1.1rem;
            }
            
            .carousel-item img {
                height: 300px;
            }
            
            .price-tag {
                font-size: 1.8rem;
            }
            
            .car-details-list b {
                min-width: 80px;
            }
        }
        
        @media (max-width: 576px) {
            .carousel-item img {
                height: 250px;
            }
            
            .details-card, .options-section {
                padding: 1.5rem;
            }
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .animated-section {
            animation: fadeIn 0.6s ease-out forwards;
        }
        
        .delay-1 {
            animation-delay: 0.2s;
        }
        
        .delay-2 {
            animation-delay: 0.4s;
        }
    </style>
</head>

<body>
    <section id="booking" class="py-5">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-10">
                    <div class="animated-section">
                        <h1 class="car-title">{{model.year}} {{model.manufacturer}} {{model.model}}</h1>
                        <div class="car-subtitle">{{model.trim}} • {{model.bodyStyle}}</div>
                    </div>

                    <div class="carousel-container animated-section delay-1">
                        <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
                            <ol class="carousel-indicators">
                                {{#each model.imagePaths}}
                                <li data-target="#carouselExampleIndicators" data-slide-to="{{@index}}" {{#if @first}}class="active"{{/if}}></li>
                                {{/each}}
                            </ol>
                            <div class="carousel-inner">
                                {{#each model.imagePaths}}
                                <div class="carousel-item {{#if @first}}active{{/if}}">
                                    <img src="{{this}}" class="d-block w-100" alt="Car Image">
                                </div>
                                {{/each}}
                            </div>
                            <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                                <span class="sr-only">Previous</span>
                            </a>
                            <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                                <span class="sr-only">Next</span>
                            </a>
                        </div>
                    </div>

                    <div class="details-card animated-section delay-1">
                        <div class="price-tag">${{model.price}}</div>
                        
                        <ul class="car-details-list">
                            <li>
                                <i class="fas fa-tachometer-alt"></i>
                                <b>Mileage:</b> {{model.mileage}} km
                            </li>
                            <li>
                                <i class="fas fa-gas-pump"></i>
                                <b>Fuel:</b> {{model.fuelType}}
                            </li>
                            <li>
                                <i class="fas fa-cogs"></i>
                                <b>Transmission:</b> {{model.transmission}}
                            </li>
                            <li>
                                <i class="fas fa-palette"></i>
                                <b>Color:</b> {{model.color}}
                                <span class="color-swatch" style="background: #{{model.color}}; width: 20px; height: 20px; display: inline-block; margin-left: 10px; border-radius: 50%; border: 1px solid #ddd;"></span>
                            </li>
                            <li>
                                <i class="fas fa-info-circle"></i>
                                <b>Status:</b> 
                                <span class="badge {{#if (eq model.carStatus 'Available')}}badge-success{{else}}badge-warning{{/if}}">
                                    {{model.carStatus}}
                                </span>
                            </li>
                            <li>
                                <i class="fas fa-car"></i>
                                <b>VIN:</b> {{model.vin}}
                            </li>
                            <li>
                                <i class="fas fa-calendar-alt"></i>
                                <b>Year:</b> {{model.year}}
                            </li>
                        </ul>
                    </div>

                    <div class="options-section animated-section delay-2">
                        <h4 class="options-title">Features & Options</h4>
                        <div class="options-container">
                            {{#each model.options}}
                            <span class="badge-option">{{this}}</span>
                            {{/each}}
                        </div>
                    </div>

                    {{#if model.description}}
                    <div class="details-card animated-section delay-2">
                        <h4 class="options-title">Description</h4>
                        <p class="car-description">{{model.description}}</p>
                    </div>
                    {{/if}}

                    <div class="text-center animated-section delay-2">
                        <button type="button" data-toggle="modal" data-target="#contactModal" class="btn-order">
                            <i class="fas fa-shopping-cart mr-2"></i> PLACE ORDER
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <div class="modal fade" id="contactModal" tabindex="-1" role="dialog" aria-labelledby="contactModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="contactModalLabel">Inquire About This Vehicle</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <form id="inquiryForm">
                        <div class="form-group">
                            <label for="username">Your Name</label>
                            <input type="text" class="form-control" id="username" placeholder="John Doe" required>
                        </div>
                        <div class="form-group">
                            <label for="useremail">Email Address</label>
                            <input type="email" class="form-control" id="useremail" placeholder="your@email.com" required>
                        </div>
                        <div class="form-group">
                            <label for="userphone">Phone Number</label>
                            <input type="tel" class="form-control" id="userphone" placeholder="123-456-7890" required>
                        </div>
                        <div class="form-group">
                            <label for="usermessage">Message (Optional)</label>
                            <textarea class="form-control" id="usermessage" rows="3" placeholder="Any specific questions about this vehicle?"></textarea>
                        </div>
                        <input type="hidden" id="carModel" value="{{model.manufacturer}} {{model.model}}">
                        <input type="hidden" id="carVin" value="{{model.vin}}">
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="submit" form="inquiryForm" class="btn btn-primary">Send Inquiry</button>
                </div>
            </div>
        </div>
    </div>

    {{#if external}}
        <a class="btn btn-view action-btn" href="/electric/booknow/external/{{this.id}}">
            <i class="fas fa-eye"></i>
        </a>
    {{else}}
        <a class="btn btn-view action-btn" href="/electric/booknow/{{this._id}}">
            <i class="fas fa-eye"></i>
        </a>
    {{/if}}

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        $(document).ready(function() {
            $('a[href*="#"]').on('click', function(e) {
                e.preventDefault();
                $('html, body').animate(
                    { scrollTop: $($(this).attr('href')).offset().top },
                    500,
                    'linear'
                );
            });
            
            $('#inquiryForm').on('submit', function(e) {
                e.preventDefault();
                const carModel = $('#carModel').val();
                alert(`Thank you for your inquiry about the ${carModel}! Our team will contact you shortly.`);
                $('#contactModal').modal('hide');
                this.reset();
            });
            
            $('.carousel').carousel({
                interval: 5000,
                pause: "hover"
            });
        });
    </script>
</body>
</html>