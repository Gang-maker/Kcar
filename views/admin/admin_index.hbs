<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard | Kcarexport</title>
    
    <!-- Favicon -->
    <link rel="icon" href="/images/auto_favi.ico" type="image/x-icon" />
    
    <!-- Font Awesome -->
    <script src="https://kit.fontawesome.com/f7b393d83e.js" crossorigin="anonymous"></script>
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Montserrat:wght@600;700&display=swap" rel="stylesheet">
    
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css">
    
    <style>
        :root {
            --primary: #2c3e50;
            --secondary: #3498db;
            --accent: #e74c3c;
            --light: #f8f9fa;
            --dark: #343a40;
            --gray: #6c757d;
            --light-gray: #e9ecef;
            --sidebar-width: 250px;
            --transition: all 0.3s ease;
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f5f7fa;
            min-height: 100vh;
            color: var(--dark);
            overflow-x: hidden;
        }
        
        /* Sidebar Styles */
        .admin-nav {
            position: fixed;
            left: 0;
            top: 0;
            width: var(--sidebar-width);
            height: 100vh;
            background: linear-gradient(135deg, var(--primary) 0%, #1a2a3a 100%);
            padding: 2rem 1rem;
            box-shadow: 5px 0 15px rgba(0,0,0,0.1);
            z-index: 1000;
            transition: var(--transition);
        }
        
        .admin-nav img {
            width: 80%;
            max-width: 180px;
            margin-bottom: 2rem;
            filter: brightness(0) invert(1);
            transition: var(--transition);
        }
        
        .admin-nav img:hover {
            transform: scale(1.05);
        }
        
        .nav-btn {
            width: 100%;
            margin: 0.5rem 0;
            padding: 0.8rem 1.5rem;
            border-radius: 6px;
            text-align: left;
            font-weight: 500;
            font-size: 1rem;
            color: white;
            background: rgba(255,255,255,0.1);
            border: none;
            transition: var(--transition);
            display: flex;
            align-items: center;
        }
        
        .nav-btn i, .nav-btn svg {
            margin-right: 10px;
            font-size: 1.1rem;
            width: 20px;
            text-align: center;
        }
        
        .nav-btn:hover {
            background: rgba(255,255,255,0.2);
            transform: translateX(5px);
            color: white;
            text-decoration: none;
        }
        
        .nav-btn.active {
            background: var(--accent);
            box-shadow: 0 4px 12px rgba(231, 76, 60, 0.3);
        }
        
        .main-content {
            margin-left: var(--sidebar-width);
            padding: 2rem;
            min-height: 100vh;
            transition: var(--transition);
        }
        
        /* Dashboard Header */
        .dashboard-header {
            background: white;
            border-radius: 10px;
            padding: 1.5rem;
            margin-bottom: 2rem;
            box-shadow: 0 2px 15px rgba(0,0,0,0.05);
        }
        
        .dashboard-title {
            font-family: 'Montserrat', sans-serif;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: 0.5rem;
        }
        
        /* Responsive Adjustments */
        @media (max-width: 992px) {
            .admin-nav {
                width: 70px;
                padding: 1rem 0.5rem;
                overflow: hidden;
            }
            
            .admin-nav img {
                width: 50px;
                margin-bottom: 1.5rem;
            }
            
            .nav-btn span {
                display: none;
            }
            
            .nav-btn i, .nav-btn svg {
                margin-right: 0;
                font-size: 1.3rem;
            }
            
            .main-content {
                margin-left: 70px;
            }
        }
        
        @media (max-width: 576px) {
            .admin-nav {
                width: 100%;
                height: auto;
                position: relative;
                padding: 1rem;
            }
            
            .admin-nav img {
                width: 120px;
                margin-bottom: 1rem;
            }
            
            .nav-container {
                display: flex;
                flex-wrap: wrap;
                justify-content: center;
            }
            
            .nav-btn {
                width: auto;
                margin: 0.3rem;
                padding: 0.6rem 1rem;
            }
            
            .nav-btn span {
                display: inline;
            }
            
            .main-content {
                margin-left: 0;
                padding: 1.5rem;
            }
        }
        
        /* Animation */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .nav-btn {
            animation: fadeIn 0.4s ease-out forwards;
        }
        
        .nav-btn:nth-child(1) { animation-delay: 0.1s; }
        .nav-btn:nth-child(2) { animation-delay: 0.2s; }
        .nav-btn:nth-child(3) { animation-delay: 0.3s; }
        .nav-btn:nth-child(4) { animation-delay: 0.4s; }
    </style>
</head>

<body>
    <!-- Admin Sidebar Navigation -->
    <div class="admin-nav">
        <div class="text-center mb-4">
            <a href="/admin/home"><img src="/images/로고 배경제거.png" alt="Kcarexport"></a>
        </div>
        
        <div class="nav-container">
            <a href="electric" class="nav-btn">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" viewBox="0 0 16 16">
                    <path fill-rule="evenodd" d="M11.251.068a.5.5 0 0 1 .227.58L9.677 6.5H13a.5.5 0 0 1 .364.843l-8 8.5a.5.5 0 0 1-.842-.49L6.323 9.5H3a.5.5 0 0 1-.364-.843l8-8.5a.5.5 0 0 1 .615-.09z"/>
                </svg>
                <span>Korean Cars</span>
            </a>
            
            <a href="gas" class="nav-btn">
                <i class="fas fa-burn"></i>
                <span>Foreign Cars</span>
            </a>
            
            <a href="customers" class="nav-btn">
                <i class="fas fa-users"></i>
                <span>Customers</span>
            </a>
            
            <a href="images" class="nav-btn">
                <i class="fas fa-cloud-upload-alt"></i>
                <span>Upload Image</span>
            </a>
        </div>
    </div>
    
    <!-- Main Content Area -->
    <div class="main-content">
        <div class="dashboard-header">
            <h1 class="dashboard-title">Admin Dashboard</h1>
            <p class="text-muted">Welcome back! Manage your inventory and customers.</p>
        </div>
        
        <!-- Dashboard content would go here -->
        <div class="alert alert-info">
            <i class="fas fa-info-circle mr-2"></i> Select a section from the sidebar to begin managing your content.
        </div>
    </div>

    <!-- jQuery, Popper.js, Bootstrap JS -->
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/js/bootstrap.min.js"></script>
    
    <script>
        $(document).ready(function() {
            // Highlight active nav button
            $('.nav-btn').each(function() {
                if (this.href === window.location.href) {
                    $(this).addClass('active');
                }
            });
            
            // Smooth transitions
            $('body').css('opacity', '1');
        });
    </script>
</body>
</html>